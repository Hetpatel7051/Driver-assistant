package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium High-Density Color Palette (Material 3 Inspired)
val HighDensityBg = Color(0xFFF7F9FC)        // Clean Slate Background
val HighDensityText = Color(0xFF1C1B1F)      // Dark Charcoal main text
val HighDensityMuted = Color(0xFF49454F)     // Medium Gray body/caption

// Brand Action Accents
val HighDensityPrimary = Color(0xFF005FB8)   // High-Density action blue
val HighDensitySecondary = Color(0xFF6750A4) // Royal Indigo-Lavender for active context
val HighDensityTertiary = Color(0xFF004A77)  // Deep Service Indigo

// Semantic Alerts (Dense contrast)
val AlertRedBg = Color(0xFFF9DEDC)           // Crimson Alert background
val AlertRedText = Color(0xFF8C1D18)         // Deep Crimson warning
val AlertBlueBg = Color(0xFFD3E3FD)          // Royal Blue Alert highlight bg
val AlertBlueText = Color(0xFF004A77)         // Deep Royal Blue alert text
val PurpleBg = Color(0xFFE8DEF8)             // Cozy Lavender highlight bg
val PurpleText = Color(0xFF21005D)           // Deep Purple text color

// Neutral boundaries
val CardBorderColor = Color(0xFFE2E8F0)      // Slate 200 light border line
val HighDensitySurface = Color(0xFFFFFFFF)   // Pure white surface
val DenseGreyBg = Color(0xFFF3F4F6)          // Soft grey-blue background accent
